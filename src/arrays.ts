// Array of numbers
// Two different ways to declare the same thing, an array of numbers
const numArray: Array<number> = [1,2,3];
// const numArray: number[] = [1,2,3];

// Array of string
// Two different ways to declare the same thing, an array of strings
const strArray: Array<string> = ['H','i','!'];
// const strArray: string[] = ['H', 'I', '!'];

console.log(strArray);
